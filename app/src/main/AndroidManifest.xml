<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Listview"
        tools:targetApi="31">

        <activity
            android:name=".ui.group_auth.SignUpActivity"
            android:exported="false"
            android:taskAffinity="com.example.create_add_layout.group_auth" />
        <activity
            android:name=".ui.UserInfoForResultActivity"
            android:exported="false"
            android:taskAffinity="com.example.create_add_layout.group_auth" />
        <activity
            android:name=".ui.group_auth.InitialActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.group_auth.HousesActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>